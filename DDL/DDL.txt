create or replace TABLE ORDER_PROCESS."ORDER_BASE".T_INVOICE (
	ORDER_ID VARCHAR(10) NOT NULL,
	ORDER_DATE DATE,
	INVOICE_DATE DATE,
	TAX_AMOUNT NUMBER(10,2),
	TOTAL_AMOUNT NUMBER(12,2),
	DISCOUNT NUMBER(10,2),
	FINAL_INVOICE_AMT NUMBER(12,2),
	CUSTOMER_ID VARCHAR(10),
	CREATE_DATE DATE,
	CREATE_USER VARCHAR(10),
	constraint PK_ORDER_ID primary key (ORDER_ID)
);

create or replace TABLE ORDER_PROCESS."ORDER_BASE".T_CUSTOMER (
	CUSTOMER_ID VARCHAR(10) NOT NULL,
	ADDRESS VARCHAR(50),
	PHONE_NO VARCHAR(10),
	CATEGORY VARCHAR(10),
	STATUS VARCHAR(10),
	TOTAL_ORDERS NUMBER(38,0),
	TOTAL_INVOICE_AMOUNT NUMBER(12,2),
        LOYALTY_POINTS NUMBER(12,2),
	CREATE_DATE DATE,
	CREATE_USER VARCHAR(10),
	constraint PK_CUSTOMER_ID primary key (CUSTOMER_ID)
);

create or replace TABLE ORDER_PROCESS."ORDER_BASE".T_SALES_ORDER_DETAIL (
	ORDER_ID VARCHAR(10) NOT NULL,
	ORDER_DATE DATE,
	CUSTOMER_ID VARCHAR(10),
	ITEM_ID VARCHAR(10),
	QTY NUMBER(38,0),
	TOTAL_AMOUNT NUMBER(12,2),
	CREATE_DATE DATE,
	CREATE_USER VARCHAR(10),
        PRICE NUMBER(12,2),
	constraint PK_ORDER_ID primary key (ORDER_ID)
);

create or replace TABLE ORDER_PROCESS."ORDER_BASE".T_SALES_ORDER_HEADER (
	ORDER_ID VARCHAR(10) NOT NULL,
	ORDER_DATE DATE,
	STATUS VARCHAR(10),
	CUSTOMER_ID VARCHAR(10),
	ITEM_COUNT NUMBER(38,0),
	TOTAL_AMOUNT NUMBER(12,2),
	DISCOUNT NUMBER(10,2),
	CREATE_DATE DATE,
	CREATE_USER VARCHAR(10),
        COUPON_CODE VARCHAR(10),
	constraint PK_ORDER_ID primary key (ORDER_ID)
);
select * from t_customer
create or replace TABLE ORDER_PROCESS."ORDER_BASE".T_PROMOTION (
	PROMOTION_ID VARCHAR(10) NOT NULL,
	CUSTOMER_CATEGORY VARCHAR(10),
	PROMOTION_TYPE VARCHAR(10),
	PROMOTION_VALUE NUMBER(10,2),
	COMMENT VARCHAR(20),
	constraint PK_PROMOTION_ID primary key (PROMOTION_ID)
);

select * from t_customer;


